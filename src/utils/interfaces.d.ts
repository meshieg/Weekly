export interface ITask {
  id: string;               // number;      ??       
  tagColor: string;         // Only the color of the tag - needs to be of tag type?
  title: string;
  date: string; //Date;
}